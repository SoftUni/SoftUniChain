export class Node {
    // Peers: URL[]
    // Blocks: Block[]
    // PendingTransactions: Transaction[]
    // Balances: map(address => number)
    // Difficulty: number
    // MiningJobs: map(address => Block)
}